import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AIM Signal Store | Veraxius",
  description:
    "Identity and signal activation. Not merchandise — a declaration system. Every position can be tested.",
};

export default function AimSignalStoreLayout({ children }: { children: React.ReactNode }) {
  return children;
}
